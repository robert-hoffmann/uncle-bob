name: quality

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  markdownlint:
    name: Markdown (markdownlint)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: arduino/setup-task@v2

      - name: Lint Markdown files
        run: task lint-md

  python-quality:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Python (ruff)
            target: lint-py
          - name: YAML (yamllint)
            target: lint-yaml
          - name: Repository Catalog
            target: test-repo-catalog
          - name: Package Metadata
            target: test-package-metadata
          - name: Repository Paths
            target: test-repo-paths
          - name: Skill Schema
            target: test-skill-schema
          - name: Governance Integrity
            target: test-governance-integrity
          - name: Governance Regression
            target: test-governance
          - name: Workflow Regression
            target: test-workflow

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@v5

      - uses: arduino/setup-task@v2

      - name: Sync Python tooling
        run: uv sync

      - name: Run ${{ matrix.target }}
        run: task ${{ matrix.target }}
